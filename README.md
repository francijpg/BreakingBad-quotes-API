# BreakingBad-quotes-API
 A simple application that allows obtaining phrases from the Breaking Bad series, built with React Hooks and the CSS Emotion library, to make custom styles in React Components.
